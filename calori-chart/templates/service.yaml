apiVersion: v1
kind: Service
metadata:
  name: {{ include "calori-chart.fullname" . }}
  labels:
    {{- include "calori-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calori-chart.selectorLabels" . | nindent 4 }}
