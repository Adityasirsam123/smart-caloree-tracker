apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aasoka-erp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "aasoka-erp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aasoka-erp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
